
# NoPlaceLikeHome

A series of config files to sync between machines.

The `sway` directory refers to the configuration for Sway, which is at `~/.config/sway`.
The `sway-commands` directory is called by the Sway configuration (usually via `exec`).
